% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_exporting.R
\name{export_data_levels}
\alias{export_data_levels}
\title{Process and Export Data Levels}
\usage{
export_data_levels(
  m_raw_level_files,
  funcs_to_apply_and_args = list(.do_filter = list(args = list(missingness_thresh_gt =
    0.3, min_plex_thresh_gt = 3), exporting = c("csv", "msnset")), .do_normalize =
    list(args = list(), exporting = c()), .do_batch_correction = list(args = list(),
    exporting = c("export_exprs", "export_msnset_to_rds"))),
  out_dir = ".",
  dry_run = FALSE
)
}
\arguments{
\item{m_raw_level_files}{Character vector. Paths to RDS files containing raw \code{MSnSet} objects.
\strong{Caveat:} Filenames must match the regex pattern \code{.*_(.*?)_level\\.RDS$}.}

\item{out_dir}{Character string. Output directory path. Defaults to current directory.}

\item{missingness_thresh_gt}{Numeric. Threshold for missingness filtering (proportion of non-NA values). Default is 0.3.}

\item{min_plex_thresh_gt}{Numeric. Minimum number of plexes a feature must be observed in. Default is 3.}
}
\value{
No return value, called for side effects (creating directories and files).
}
\description{
Iterates through a set of raw data files, performs filtering, normalization, and batch correction,
and exports the results.
}
